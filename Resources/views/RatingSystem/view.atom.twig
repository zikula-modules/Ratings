{# purpose of this template: rating systems atom feed #}
<?xml version="1.0" encoding="{% set charset = pageGetVar('meta.charset') %}{% if charset == 'ISO-8859-15' %}ISO-8859-1{% else %}{{ charset }}{% endif %}" ?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <title type="text">{{ __('Latest rating systems') }}</title>
    <subtitle type="text">{{ __('A direct feed showing the list of rating systems') }} - {{ getSystemVar('slogan') }}</subtitle>
    <author>
        <name>{{ getSystemVar('sitename') }}</name>
    </author>
{% set amountOfItems = items|length %}
{% if amountOfItems > 0 %}
{% set uniqueID %}tag:{{ app.request.schemeAndHttpHost|replace({'http://': '', '/': ''}) }},{{ items.first.createdDate|date('Y-m-d') }}:{{ path('paustianratingsmodule_ratingsystem_' ~ routeArea ~ 'view') }}{% endset %}
    <id>{{ uniqueID }}</id>
    <updated>{{ items[0].updatedDate|date('Y-m-dTH:M:SZ') }}</updated>
{% endif %}
    <link rel="alternate" type="text/html" hreflang="{{ app.request.locale }}" href="{{ url('paustianratingsmodule_ratingsystem_' ~ routeArea ~ 'index') }}" />
    <link rel="self" type="application/atom+xml" href="{{ app.request.schemeAndHttpHost ~ app.request.basePath }}" />
    <rights>Copyright (c) {{ 'now'|date('Y') }}, {{ app.request.schemeAndHttpHost }}</rights>
{% for ratingSystem in items %}
    <entry>
        <title type="html">{{ ratingSystem|paustianratingsmodule_formattedTitle }}</title>
        <link rel="alternate" type="text/html" href="{{ url('paustianratingsmodule_ratingsystem_' ~ routeArea ~ 'view') }}" />
        {% set uniqueID %}tag:{{ app.request.schemeAndHttpHost|replace({ 'http://': '', '/': '' }) }},{{ ratingSystem.createdDate|date('Y-m-d') }}:{{ path('paustianratingsmodule_ratingsystem_' ~ routeArea ~ 'view') }}{% endset %}
        <id>{{ uniqueID }}</id>
        {% if ratingSystem.updatedDate|default %}
            <updated>{{ ratingSystem.updatedDate|date('Y-m-dTH:M:SZ') }}</updated>
        {% endif %}
        {% if ratingSystem.createdDate|default %}
            <published>{{ ratingSystem.createdDate|date('Y-m-dTH:M:SZ') }}</published>
        {% endif %}
        {% if ratingSystem.createdBy|default and ratingSystem.createdBy.getUid() > 0 %}
            {% set creatorAttributes = ratingSystem.createdBy.getAttributes() %}
            <author>
               <name>{{ creatorAttributes.get('realname')|default(creatorAttributes.get('name'))|default(ratingSystem.createdBy.getUname()) }}</name>
               <uri>{{ creatorAttributes.get('_UYOURHOMEPAGE')|default('-') }}</uri>
               <email>{{ ratingSystem.createdBy.getEmail() }}</email>
            </author>
        {% endif %}
        <summary type="html">
            <![CDATA[
            {{ ratingSystem.iconFa|truncate(150, true, '&hellip;')|default('-') }}
            ]]>
        </summary>
        <content type="html">
            <![CDATA[
            {{ ratingSystem|paustianratingsmodule_formattedTitle|replace({ '<br>': '<br />' }) }}
            ]]>
        </content>
    </entry>
{% endfor %}
</feed>
